# PROJECT_NAME

Short description of this project.

## Setup

```bash
cp .env.example .env
# edit .env — never commit it
```

## Security

Do not commit `.env`, private keys, or tokens. Run `check-secrets` before pushing.
