# PROJECT_NAME

Homelab / Linux server project. Intended to be deployed over Tailscale SSH.

## Setup

```bash
cd PROJECT_NAME
cp .env.example .env
```

Do not expose SSH or admin ports on the public internet. Prefer Tailscale.
