# PROJECT_NAME

Node.js project. Dependencies are local (`npm install` in this directory).

## Setup

```bash
cd PROJECT_NAME
nvm use
npm install
cp .env.example .env
```

## Commands

```bash
npm test
npm start
```

Do not install packages globally unless there is a strong reason.
