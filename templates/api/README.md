# PROJECT_NAME

HTTP API skeleton (Python). Add FastAPI/Flask yourself when you need it.

## Setup

```bash
cd PROJECT_NAME
uv venv
source .venv/bin/activate
uv pip install -e ".[dev]"
cp .env.example .env
uv run python -m app
```
